# =============================================================================
# Godot 4.x 项目 .gitignore
# 只上传代码，忽略二进制和生成文件
# =============================================================================

# -----------------------------------------------------------------------------
# Godot 编辑器生成的文件
# -----------------------------------------------------------------------------
.godot/
# 编辑器配置、导入资源缓存、临时文件
assets/
# -----------------------------------------------------------------------------
# 导入的资源文件（.import）
# -----------------------------------------------------------------------------
*.import
# .import 文件是 Godot 自动生成的，包含导入设置
# 原始文件（.png, .ogg 等）会被重新导入

# -----------------------------------------------------------------------------
# 导出的构建文件
# -----------------------------------------------------------------------------
/export/
# 包含导出的游戏可执行文件和平台特定文件

/android/
/export_android/
# Android 导出文件

/ios/
/export_ios/
# iOS 导出文件

/web/
/export_web/
# Web 导出文件

/windows/
/desktop/
# 桌面平台导出文件

# -----------------------------------------------------------------------------
# 临时文件和缓存
# -----------------------------------------------------------------------------
*.tmp
*.temp
~$*

# -----------------------------------------------------------------------------
# 操作系统生成的文件
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# IDE 和编辑器
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# -----------------------------------------------------------------------------
# 用户特定的项目设置（可选）
# -----------------------------------------------------------------------------
# 如果你想要保留项目设置，注释掉下面这行
# project.godot

# -----------------------------------------------------------------------------
# 调试和日志文件
# -*-
*.log
*.trace

# -----------------------------------------------------------------------------
# 文档和笔记（如果不想上传）
# -----------------------------------------------------------------------------
# *.md
# /docs/

# -----------------------------------------------------------------------------
# 备份文件
# -----------------------------------------------------------------------------
*.bak
*.backup

# -----------------------------------------------------------------------------
# 压缩文件和归档
# -----------------------------------------------------------------------------
*.zip
*.rar
*.7z
*.tar
*.gz

# -----------------------------------------------------------------------------
# 注意事项
# -----------------------------------------------------------------------------
# 应该上传的文件：
# ✓ .gd 文件（GDScript 脚本）
# ✓ .tscn 文件（场景文件，文本格式）
# ✓ .tres 文件（资源文件，文本格式）
# ✓ .godot 文件（项目配置，文本格式）
# ✓ 原始资源文件（.png, .jpg, .ogg, .wav 等）
# ✓ .md 文档（如果有）
#
# 不应该上传的文件：
# ✗ .import 文件（自动生成）
# ✗ .godot/ 文件夹（编辑器缓存）
# ✗ 导出的可执行文件
# ✗ 二进制大文件（如 .blend, .psd，除非版本控制）
# -----------------------------------------------------------------------------
